# Server Configuration
# Port for the email service to run on
PORT=5002
# Environment: development, production, or test
NODE_ENV=development

# Email Configuration (SMTP)
# SMTP server configuration for sending emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
# Gmail account or your email provider credentials
SMTP_USER=your-email@gmail.com
# For Gmail, use an app-specific password: https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# Admin Email Configuration
# Email address where admin notifications will be sent
ADMIN_EMAIL=admin@mayhemcreations.com
# Display name for admin emails
ADMIN_NAME=Mayhem Creations Admin

# Frontend URL Configuration
# URL of the frontend application (used for email links)
FRONTEND_URL=http://localhost:5173

# Main Backend Configuration
# URL of the main backend service
MAIN_BACKEND_URL=http://localhost:5001
# Secret key for webhook authentication (must match the main backend)
MAIN_BACKEND_WEBHOOK_SECRET=your-webhook-secret-key

# Logging Configuration
# Options: error, warn, info, debug
LOG_LEVEL=info

# Email Service Features
# This service handles the following email types:
# - Chat notifications (customer messages, admin replies, conversation summaries)
# - Order confirmations (when orders are placed)
# - Shipping confirmations (with tracking information)
# - Delivery notifications (when orders are delivered)
# - Refund confirmations (when refunds are processed)
# - Payment receipts (transaction confirmations)
# - Review requests (asking for product reviews)
# - Newsletters (marketing and promotional content)
# - Account updates (security alerts, profile changes)
